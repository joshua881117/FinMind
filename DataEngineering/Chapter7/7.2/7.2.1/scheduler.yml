version: '3.0'
services:
  scheduler:
    image: crawler:7.2.1
    hostname: "twse"
    command: pipenv run python financialdata/scheduler.py
    restart: always
    environment:
      - TZ=Asia/Taipei
    networks:
        - test

networks:
  test:
    # 加入已經存在的網路
    external: true
